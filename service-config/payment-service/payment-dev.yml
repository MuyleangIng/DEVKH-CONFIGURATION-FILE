spring:
  application:
    name: DEVKH-PAYMENT-SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/payment-service
    username: postgres
    password: kotreyjr10
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: create
