server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oauth2_Developers-Cambodia
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
